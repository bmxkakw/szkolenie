// EXERCISE 5: Passing function as an argument

function toFixed (amount) {
    // write a function that returns amount rounded to 2 decimal digits
}

function addVat (netAmount) {
    // write a function that returns amount increased by 23% VAT
}

function applyDiscount (amount) {
    // write a function that returns amount decreased by 10%
}

function processAmount (amount, processingFunc) {
    // write a function that takes two arguments: a value and a function to process it.
    // If the first argument is a valid number, return the result of processing it by the passed function.
    // In case the first argument is not a valid number, return null.
    // You can use isNaN function to validate numbers and parseFloat function to convert strings to numbers
}
